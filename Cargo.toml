[workspace]
resolver = "2"
members = ["musicforge-core", "musicforge-cli", "musicforge-gui/src-tauri"]

[workspace.package]
license = "MIT"
edition = "2021"

[profile.release]
lto = true
opt-level = "z"
strip = true
panic = "abort"  # 硬约束 1：零 panic
